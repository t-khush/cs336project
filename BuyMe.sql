#CREATE DATABASE  IF NOT EXISTS `BuyMe` /*!40100 DEFAULT CHARACTER SET latin1 */;
USE `BuyMe`;

#create table user (username varchar(30), password varchar(30));
#insert into user (username, password) values ("plolla", "hello");

select * from user;

